# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

directive @upper on FIELD_DEFINITION

"""price"""
type Price {
  id: ID!
  description: String
  amount: Int
  title: String!
}

"""recipe"""
type Attribute {
  id: ID!
  value: String!
}

"""Product"""
type Product {
  id: ID!
  prices: [Price!]
  description: String
  gallery: [String!]
  inStock: Boolean
  brand: String
}

"""category"""
type Category {
  id: ID!
  name: String!
  products: [Product!]!
}

type Query {
  category(id: String!): Category!
  categories(skip: Int = 0, take: Int = 25): [Category!]!
  product(id: String!): Product!
  products(skip: Int = 0, take: Int = 25): [Product!]!
  price(id: String!): Price!
  prices(skip: Int = 0, take: Int = 25): [Price!]!
  attribute(id: String!): Attribute!
  attributes(skip: Int = 0, take: Int = 25): [Attribute!]!
}

type Mutation {
  addCategory(newCategoryInput: NewCategoryInput!): Category!
  removeCategory(id: String!): Boolean!
  addProduct(newProductData: NewProductInput!): Product!
  removePrice(id: String!): Boolean!
  addPrice(newPriceInput: NewPriceInput!): Price!
  addAttribute(newAttributeInput: NewAttributeInput!): Attribute!
  removeAttribute(id: String!): Boolean!
}

input NewCategoryInput {
  name: String!
}

input NewProductInput {
  description: String
  inStock: Boolean!
  brand: String
}

input NewPriceInput {
  title: String!
  description: String
  amount: Int
}

input NewAttributeInput {
  title: String!
  value: String!
}